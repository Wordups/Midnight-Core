"""Document .docx validators.

`schema_validator` checks that an exported file is a syntactically valid
.docx with a recognizable internal structure. It backs the live export
path's integrity check (see backend/api/routes.py).
"""
